<UserControl x:Class="NineWorldsDeep.Tapestry.NodeUI.ClusterNodeDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NineWorldsDeep.Tapestry.NodeUI"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400">

    <Grid>

        <ContentControl x:Name="ccClusterChooser"
                        Visibility="Visible">
            <local:ClusterChooser x:Name="clusterChooser"/>
        </ContentControl>

        <ContentControl x:Name="ccClusterDisplay"
                        Visibility="Collapsed">
            <local:ClusterDisplay x:Name="clusterDisplay"/>
        </ContentControl>

    </Grid>
    
</UserControl>
